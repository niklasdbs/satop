#include "TOPEnv.h"

int main(){
    // TOPEnv env = TOPEnv(TRAINING);
    // env.reset(false, false);

    // while(true){
    //     env.step(4);

    //     int a = 3;
    // }
/*    auto obs_0 = env.reset(true);
    while (true) {
        auto [obs, reward, done, infos] = env.step(0);
        if (done) {
            //break;
            env.reset(false);
        }
    }
    */
    return 0;
}
